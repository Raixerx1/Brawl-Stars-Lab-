import type {Metadata} from "next";import FavoritesClient from "@/components/FavoritesClient";import {maps,brawlers} from "@/lib/data";export const metadata:Metadata={title:"Favoritos"};export default function FavoritesPage(){return <div className="page"><div className="page-heading"><span className="eyebrow">Acceso rápido</span><h1>Favoritos</h1><p>Se guardan localmente en tu navegador.</p></div><FavoritesClient maps={maps} brawlers={brawlers}/></div>}
