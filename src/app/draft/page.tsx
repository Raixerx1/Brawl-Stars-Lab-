import type {Metadata} from "next";import DraftAssistant from "@/components/DraftAssistant";import {maps,brawlers} from "@/lib/data";export const metadata:Metadata={title:"Draft Assistant"};export default function DraftPage(){return <div className="page"><div className="page-heading"><span className="eyebrow">Motor heurístico v0.1</span><h1>Draft Assistant</h1><p>Analiza mapa, orden, aliados, enemigos y bans. Devuelve una puntuación estratégica, no una falsa probabilidad.</p></div><DraftAssistant maps={maps} brawlers={brawlers}/></div>}
