import type {Metadata} from "next";import {meta} from "@/lib/data";export const metadata:Metadata={title:"Fuentes"};export default function SourcesPage(){return <div className="page"><div className="page-heading"><span className="eyebrow">Transparencia</span><h1>Fuentes y metodología</h1><p>Cada dato cambiante debe conservar su procedencia y fecha.</p></div><div className="source-list">{meta.sources.map((s:{name:string;url:string;kind:string})=><a href={s.url} target="_blank" rel="noreferrer" key={s.url} className="panel source-card"><span>{s.kind}</span><h2>{s.name}</h2><p>{s.url}</p></a>)}</div><div className="notice spaced">Brawl Time Ninja y Brawlify son fuentes de terceros. La aplicación debe mostrar sus limitaciones y nunca mezclar tasas de victoria de muestras distintas como si fueran equivalentes.</div></div>}
