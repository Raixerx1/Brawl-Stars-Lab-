import {notFound} from "next/navigation";import Link from "next/link";import {maps,mapBySlug,brawlerByName} from "@/lib/data";import BrawlerCard from "@/components/BrawlerCard";import FavoriteButton from "@/components/FavoriteButton";
export function generateStaticParams(){return maps.map(m=>({slug:m.slug}))}
export default async function MapDetail({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const map=mapBySlug(slug);if(!map)notFound();return <div className="page"><Link href="/maps" className="back-link">← Todos los mapas</Link><section className="detail-hero"><div><div className="card-kicker">{map.mode}</div><h1>{map.name}</h1><p>{map.status}</p><div className="tag-row">{map.traits.map(t=><span key={t}>{t}</span>)}</div></div><FavoriteButton type="map" id={map.slug}/></section><div className="detail-grid"><section className="panel"><span className="eyebrow">Draft</span><h2>Prioridades</h2><div className="draft-columns"><div><h3>First picks</h3>{map.firstPicks.map((x,i)=><p key={x}><b>{i+1}.</b> {x}</p>)}</div><div><h3>Last picks</h3>{map.lastPicks.map((x,i)=><p key={x}><b>{i+1}.</b> {x}</p>)}</div><div><h3>Bans</h3>{map.bans.map((x,i)=><p key={x}><b>{i+1}.</b> {x}</p>)}</div></div></section><section className="panel plan-panel"><span className="eyebrow">Condición de victoria</span><h2>Plan de partida</h2><p>{map.plan}</p><Link href={`/draft?map=${map.slug}`} className="secondary-button">Analizar un draft</Link></section></div><div className="section-title spaced"><div><span className="eyebrow">Tier S</span><h2>Mejores opciones</h2></div></div><div className="card-grid brawler-grid">{map.tierS.map(n=>brawlerByName(n)).filter(Boolean).map(b=><BrawlerCard key={b!.slug} brawler={b!}/>)}</div><div className="section-title spaced"><div><span className="eyebrow">Tier A</span><h2>Alternativas sólidas</h2></div></div><div className="card-grid brawler-grid">{map.tierA.map(n=>brawlerByName(n)).filter(Boolean).map(b=><BrawlerCard key={b!.slug} brawler={b!}/>)}</div><div className="notice">No se muestran win rates inventados. Los tiers son una síntesis editorial y deben revisarse con cada parche.</div></div>}
