import type {Metadata} from "next";import MapExplorer from "@/components/MapExplorer";import {maps,modes} from "@/lib/data";export const metadata:Metadata={title:"Mapas"};export default function MapsPage(){return <div className="page"><div className="page-heading"><span className="eyebrow">Guía competitiva</span><h1>Mapas</h1><p>Tier editorial, first picks, last picks, bans y plan de partida.</p></div><MapExplorer maps={maps} modes={modes}/></div>}
