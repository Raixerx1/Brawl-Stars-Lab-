import Link from "next/link";import type { Brawler } from "@/lib/types";import FavoriteButton from "./FavoriteButton";
export default function BrawlerCard({brawler}:{brawler:Brawler}){return <article className="card brawler-card"><FavoriteButton type="brawler" id={brawler.slug}/><Link href={`/brawlers/${brawler.slug}`}><div className="avatar">{brawler.name.slice(0,2).toUpperCase()}</div><div><div className="card-kicker">{brawler.rarity}</div><h3>{brawler.name}</h3><p>{brawler.role} · {brawler.range}</p></div><span className={`tier tier-${brawler.tier.replace('+','p').replace(' ','-').toLowerCase()}`}>{brawler.tier}</span></Link></article>}
