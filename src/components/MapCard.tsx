import Link from "next/link";import type { MapProfile } from "@/lib/types";import FavoriteButton from "./FavoriteButton";
export default function MapCard({map}:{map:MapProfile}){return <article className="card map-card"><FavoriteButton type="map" id={map.slug}/><Link href={`/maps/${map.slug}`}><div className="card-kicker">{map.mode}</div><h3>{map.name}</h3><p>{map.layout} · {map.traits.slice(0,2).join(' · ')}</p><div className="mini-row"><span>S: {map.tierS.slice(0,2).join(', ')}</span><span>{map.featuredOfficialJune2026?'Oficial destacado':'Referencia'}</span></div></Link></article>}
